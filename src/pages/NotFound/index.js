import React from 'react';
import { GeneralApp } from '../../Componets/GeneralApp';

function NotFound() {

  return (
    <GeneralApp route={"/"}>
      <p>NotFound</p>
    </GeneralApp>
  );
}

export default NotFound;